import { StyleSheet, Text, View } from 'react-native'

const FindScreen = () => {
  return (
    <View>
      <Text style={{color: '#fff', fontSize: 20}}>FindScreen</Text>
    </View>
  )
}

export default FindScreen

const styles = StyleSheet.create({})